type Mutation {
  createApplication(
    motive: String
    spec: String
    activity: String
    experience: String
    wannaMakeDesc: String
    wannaMakeImageUrl: String
    volunteerEmail: String!
    volunteerPassword: String!
  ): Application!
}
